# Nextflow runtime
.nextflow/
.nextflow.log*
work/
work_*/

# Pipeline outputs (all run-specific result dirs)
results/
results_*/

# Per-run launch staging + scratch run dirs
.launch_*/
test_clonetracer_gpu/

# SLURM / orchestrator scratch
gpu_smoke_*
*.jobid
slurm-*.out

# Stray plugin report defaults (in case a config path is ever misconfigured)
co2footprint_report_*.html
co2footprint_summary_*.txt
co2footprint_trace_*.txt
execution_*.html
execution_*.txt
pipeline_dag_*.html
nfprov_*.json

# Input data, references, built containers (live on scratch/Longship, never git)
data/
data_test/
test_data/
references/
refs/
*.sif
*.img
logs/

# Clinical / biobank patient metadata — never commit (data governance)
assets/Paed_AML_Vivobank_details.csv
*Vivobank*
*_details.csv

# Secrets — never commit
*api_key*
*.key
.netrc
credentials*
token*

# Large data file types (belt & braces)
*.bam
*.cram
*.sam
*.bai
*.crai
*.fastq
*.fq
*.fastq.gz
*.fq.gz
*.sra
*.sra.tmp
*.vcf
*.vcf.gz
*.bcf
*.h5
*.h5ad
*.rds
*.RDS
*.loom
*.mtx

# OS / editor / python
.DS_Store
__pycache__/
*.pyc
.ipynb_checkpoints/
